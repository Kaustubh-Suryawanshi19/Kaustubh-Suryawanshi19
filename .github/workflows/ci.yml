name: CI

on:
  push:
  pull_request:

jobs:
  profile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate profile
        run: |
          test -f README.md
          test -d assets
          echo "Profile repository validation passed."
